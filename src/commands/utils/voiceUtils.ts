import { VoiceConnection } from "@discordjs/voice";

export const getConnections = (guildId: number): Promise<VoiceConnection> => {
    return new Promise<VoiceConnection>((resolve, reject) => {

    });
};

export const 